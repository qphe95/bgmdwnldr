# LLDB Commands for QuickJS Shape Corruption Debugging
# 
# Usage:
# 1. Run ./lldb_debug2.sh in one terminal
# 2. In another terminal, run: lldb -s lldb_commands.txt

# Settings
settings set target.async false
settings set target.x86-disassembly-flavor intel

# Connect to device
platform select remote-android
platform connect connect://localhost:5039

# Get PID from logcat or script output
# We'll attach to the running process
script print("=== QuickJS Shape Corruption Debugger ===")
script print("Attach to process using: attach -p <PID>")
script print("Then: command source lldb_commands_attach.txt")
